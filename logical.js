let country = Columbia;
const language = Spanish;
const population = 44; 


if (language === 'English' && population < 55 && !isisland) {
    console.log(`You should live in ${country}:)`)
} else {
    console.log(`${country} doesn't meet your criteria:(`)
}